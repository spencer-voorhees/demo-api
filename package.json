{
  "name": "demo-api",
  "version": "1.0.0",
  "description": "Demo order API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "NODE_ENV=test jest --testEnvironment=node"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^7.2.2"
  }
}
